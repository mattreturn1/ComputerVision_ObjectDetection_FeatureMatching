#ifndef FILTER_HPP
#define FILTER_HPP

#include <opencv2/core/mat.hpp>
#include <opencv2/opencv.hpp>

using namespace cv;
using namespace std;

Mat preprocessImage(const Mat &image);

#endif //FILTER_HPP
